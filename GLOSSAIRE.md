# 🧳 Valise de Secours : Glossaire des Syntaxes

Pour ne pas s'emmêler les pinceaux entre le Web (JavaScript) et le Backend/Mobile (Kotlin).

| Concept | JavaScript (React) | Kotlin (Spring/Android) | Analogie |
| :--- | :--- | :--- | :--- |
| **Variable fixe** | `const nom = "Jean"` | `val nom = "Jean"` | Une étiquette collée. |
| **Variable changeante** | `let score = 0` | `var score = 0` | Un tableau à craie. |
| **Fonction / Action** | `function direBonjour() { ... }` | `fun direBonjour() { ... }` | Une recette de cuisine. |
| **Afficher un message** | `console.log("Coucou")` | `println("Coucou")` | Crier dans un mégaphone. |
| **Liste / Tableau** | `[1, 2, 3]` | `listOf(1, 2, 3)` | Un compartiment à œufs. |
| **Si / Alors** | `if (age > 18) { ... }` | `if (age > 18) { ... }` | Un embranchement de route. |

### 💡 Le petit truc en plus
- En **React**, on utilise souvent le `fetch` pour parler au serveur.
- En **Android**, on préfère **Retrofit**, qui est un outil plus puissant pour faire la même chose proprement.
- En **Kotlin**, on ne met pas de point-virgule `;` à la fin des lignes (c'est possible mais pas obligatoire).
